name: Pull Request

on:
  pull_request:
    paths:
      - 'packs/**'

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  build:
    name: Get changed packs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Get changed files
        id: changed-files
        uses: jitterbit/get-changed-files@v1
        with:
          format: json

      - name: Unzip files from 'packs' directory
        run: |
          echo '${{ steps.changed-files.outputs.files }}' | jq -r '.[]' | grep 'packs/' | xargs -I {} unzip -o {} -d tmp/{}
          find tmp/ -name 'pack.meta' -exec cat {} \; | jq -r '.name' | tr '' ',' >> packs.md

      - name: Log contents of markdown file
        run: |
          cat packs.md

      - name: Create comment
        uses: peter-evans/create-or-update-comment@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body-file: packs.md
          reactions: eyes
